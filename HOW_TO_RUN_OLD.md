# 🚀 كيفية تشغيل المشروع

## الطريقة الأولى: Live Server (VS Code)

1. تنصيب Extension:
   - افتح VS Code
   - اضغط Extensions (Ctrl+Shift+X)
   - دور على "Live Server"
   - اضغط Install

2. تشغيل المشروع:
   - افتح ملف `index.html`
   - Right Click → "Open with Live Server"
   - أو اضغط Alt+L ثم Alt+O

3. المتصفح هيفتح تلقائياً على:
   `http://localhost:5500` أو `http://127.0.0.1:5500`

## الطريقة الثانية: Python Server

```bash
# Python 3
python -m http.server 8000

# افتح المتصفح على:
http://localhost:8000
```

## الطريقة الثالثة: PHP Server

```bash
php -S localhost:8000

# افتح المتصفح على:
http://localhost:8000
```

## الطريقة الرابعة: Node.js Server

```bash
npx http-server -p 8000

# افتح المتصفح على:
http://localhost:8000
```

## ✅ مميزات In-Memory Storage:
- ✅ مش محتاج MySQL
- ✅ مش محتاج PHP
- ✅ مش محتاج أي database setup
- ✅ شغال بـ HTML + CSS + JavaScript بس
- ✅ سريع جداً ⚡

## ⚠️ ملحوظات:
- البيانات مؤقتة (في الذاكرة)
- الـ Cart بيتحفظ في localStorage (يفضل موجود)
- لو عايز البيانات تفضل، استخدم localStorage للكل
- Admin Dashboard متاح على `/pages/admin/index.html`
- يمكنك إضافة أو حذف الكتب من خلال الـ Admin Dashboard

## 📁 هيكل المشروع:

```
bookstore/
├── index.html              # الصفحة الرئيسية
├── pages/
│   ├── shop.html          # صفحة المتجر
│   ├── book-details.html  # تفاصيل الكتاب
│   ├── cart.html          # سلة التسوق
│   └── admin/
│       └── index.html     # لوحة التحكم
├── assets/
│   ├── css/
│   │   └── style.css      # ملف التنسيق
│   └── js/
│       ├── storage.js     # نظام التخزين في الذاكرة
│       └── main.js        # الوظائف الرئيسية
└── HOW_TO_RUN.md          # هذا الملف
```

## 🎯 الميزات المتاحة:

1. **تصفح الكتب**: شاهد جميع الكتب المتاحة مع إمكانية البحث والتصفية
2. **سلة التسوق**: إضافة وإدارة الكتب في السلة
3. **تفاصيل الكتاب**: عرض معلومات تفصيلية عن كل كتاب
4. **لوحة التحكم**: إدارة المخزون (إضافة/تعديل/حذف الكتب)
5. **التصنيفات**: تصفح الكتب حسب الفئة
6. **البحث**: البحث عن الكتب بالعنوان أو المؤلف أو الفئة

## 🔧 التخصيص:

لتعديل الكتب أو إضافة كتب جديدة، افتح ملف `assets/js/storage.js` وعدّل مصفوفة `this.books` في الـ constructor.

مثال:
```javascript
{
    id: 9,
    title: "كتابك الجديد",
    author: "المؤلف",
    price: 19.99,
    category: "Fiction",
    isbn: "978-1-234-56789-0",
    stock: 10,
    description: "وصف الكتاب...",
    image: "https://example.com/image.jpg",
    rating: 4.5
}
```
